/**
 * FreeCodeCamp - Redux: Lesson 04
 *
 * Define a Redux Action
 */

/**
 * Defining a Redux Action with the type of 'LOGIN'.
 * NOTE: This could be referred to later and have logic that would define what happens when a user
 * logins, which would call the action with the type 'LOGIN', and thus define what changes to state
 * are made in that case. This will be reviewed (defined) later.
 */
const action = {
  type: 'LOGIN'
}
